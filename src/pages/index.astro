---
import { chronosHomeRedirect } from '../server/app/route-contract';

const homeRedirect = chronosHomeRedirect;

if (homeRedirect.location.length === 0) {
  throw new Error('Home redirect location is required.');
}

return Astro.redirect(homeRedirect.location, homeRedirect.status);
---
