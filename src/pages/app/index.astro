---
import { chronosAppIndexRedirect } from '../../server/app/route-contract';

if (chronosAppIndexRedirect.kind === 'redirect') {
  return Astro.redirect(chronosAppIndexRedirect.location, chronosAppIndexRedirect.status);
}
---
